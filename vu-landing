<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>VÜ — one pause changes the view.</title>
<link href="https://api.fontshare.com/v2/css?f[]=general-sans@300,400,500,600&display=swap" rel="stylesheet">
<style>
  :root{
    --black:#000000;
    --ink:#0a0a08;
    --cream:#FFFBED;
    --cream-dim:rgba(255,251,237,.62);
    --cream-faint:rgba(255,251,237,.14);
    --white:#ffffff;
    --rainbow:linear-gradient(180deg,#ff3b30 0%,#ff9500 18%,#ffe14d 36%,#4cd964 55%,#38b6ff 74%,#af52de 92%);
    --rainbow-h:linear-gradient(90deg,#ff3b30,#ff9500,#ffe14d,#4cd964,#38b6ff,#af52de);
    --ease:cubic-bezier(.22,1,.36,1);
  }
  *{margin:0;padding:0;box-sizing:border-box;}
  html{scroll-behavior:smooth;}
  body{
    background:var(--black);
    color:var(--cream);
    font-family:'General Sans',-apple-system,'Helvetica Neue',sans-serif;
    font-weight:400;
    -webkit-font-smoothing:antialiased;
    overflow-x:hidden;
  }
  ::selection{background:var(--cream);color:var(--black);}

  .label{
    font-size:11px;
    letter-spacing:.32em;
    text-transform:uppercase;
    font-weight:500;
  }

  /* ---------- NAV ---------- */
  nav{
    position:fixed;inset:0 0 auto 0;z-index:50;
    display:flex;align-items:center;justify-content:space-between;
    padding:26px clamp(24px,5vw,64px);
    mix-blend-mode:difference;
  }
  .logo{
    color:var(--white);text-decoration:none;
    font-size:22px;font-weight:600;letter-spacing:.14em;
    display:flex;align-items:baseline;gap:2px;
  }
  .nav-links{display:flex;gap:36px;align-items:center;}
  .nav-links a{
    color:var(--white);text-decoration:none;
    font-size:11px;letter-spacing:.28em;text-transform:uppercase;font-weight:500;
    opacity:.75;transition:opacity .3s;
  }
  .nav-links a:hover,.nav-links a:focus-visible{opacity:1;}
  .nav-cta{
    border:1px solid rgba(255,255,255,.5)!important;
    border-radius:999px;padding:10px 22px;opacity:1!important;
  }
  @media(max-width:720px){.nav-links a:not(.nav-cta){display:none;}}

  /* ---------- HERO ---------- */
  .hero{
    min-height:100svh;
    display:flex;flex-direction:column;
    align-items:center;justify-content:center;
    text-align:center;
    padding:120px 24px 80px;
    position:relative;
  }
  .prism{
    position:relative;
    width:min(760px,92vw);
    height:clamp(200px,32vw,300px);
    margin-bottom:clamp(40px,6vh,72px);
  }
  .beam{
    position:absolute;
    top:6%;
    right:calc(50% + 26px);
    width:46%;
    height:2px;
    background:linear-gradient(90deg,transparent,rgba(255,255,255,.95));
    box-shadow:0 0 12px rgba(255,255,255,.8),0 0 30px rgba(255,255,255,.35);
    transform-origin:right center;
    transform:rotate(14deg);
    animation:beamIn 1.4s var(--ease) .2s both;
  }
  @keyframes beamIn{from{opacity:0;scale:0 1;}to{opacity:1;scale:1 1;}}
  .bars{
    position:absolute;left:50%;top:50%;
    transform:translate(-50%,-50%);
    display:flex;gap:20px;
  }
  .bar{
    width:clamp(20px,3.4vw,30px);
    height:clamp(140px,22vw,210px);
    background:var(--white);
    border-radius:5px;
    box-shadow:
      0 0 18px rgba(255,255,255,.9),
      0 0 60px rgba(255,255,255,.45),
      0 0 140px rgba(255,255,255,.22);
    animation:barIn 1.2s var(--ease) .7s both;
  }
  .bar:nth-child(2){animation-delay:.85s;}
  @keyframes barIn{from{opacity:0;transform:scaleY(.2);}to{opacity:1;transform:scaleY(1);}}
  .spectrum{
    position:absolute;
    left:calc(50% + 34px);
    top:50%;
    width:52%;
    height:78%;
    transform:translateY(-50%);
    background:var(--rainbow);
    clip-path:polygon(0 46%,100% 0,100% 100%,0 54%);
    filter:blur(10px) saturate(1.15);
    opacity:0;
    animation:spectrumIn 1.8s var(--ease) 1.5s forwards;
    will-change:transform,opacity;
  }
  @keyframes spectrumIn{to{opacity:.92;}}
  .hero .label{color:var(--cream-dim);animation:fadeUp 1s var(--ease) 1.9s both;}
  h1{
    font-size:clamp(34px,5.4vw,64px);
    font-weight:500;
    letter-spacing:-.01em;
    line-height:1.08;
    margin:18px 0 20px;
    animation:fadeUp 1s var(--ease) 2.05s both;
  }
  .hero p{
    max-width:420px;
    color:var(--cream-dim);
    font-size:16px;line-height:1.7;font-weight:300;
    animation:fadeUp 1s var(--ease) 2.2s both;
  }
  .hero-ctas{
    display:flex;gap:16px;flex-wrap:wrap;justify-content:center;
    margin-top:40px;
    animation:fadeUp 1s var(--ease) 2.35s both;
  }
  @keyframes fadeUp{from{opacity:0;transform:translateY(16px);}to{opacity:1;transform:translateY(0);}}
  .btn{
    display:inline-flex;align-items:center;gap:10px;
    padding:15px 32px;border-radius:999px;
    font-size:11px;letter-spacing:.28em;text-transform:uppercase;font-weight:500;
    text-decoration:none;cursor:pointer;
    transition:transform .35s var(--ease),box-shadow .35s var(--ease),background .35s,color .35s;
  }
  .btn-solid{background:var(--cream);color:var(--black);border:1px solid var(--cream);}
  .btn-solid:hover{transform:translateY(-2px);box-shadow:0 0 34px rgba(255,251,237,.35);}
  .btn-ghost{background:transparent;color:var(--cream);border:1px solid var(--cream-faint);}
  .btn-ghost:hover{border-color:var(--cream);}
  .btn:focus-visible{outline:2px solid var(--cream);outline-offset:3px;}

  .scroll-hint{
    position:absolute;bottom:28px;left:50%;transform:translateX(-50%);
    width:1px;height:44px;overflow:hidden;
  }
  .scroll-hint::after{
    content:'';position:absolute;inset:0;
    background:linear-gradient(var(--cream),transparent);
    animation:drip 2.2s ease-in-out infinite;
  }
  @keyframes drip{0%{transform:translateY(-100%);}60%,100%{transform:translateY(100%);}}

  /* ---------- SYSTEM (cream) ---------- */
  .system{
    background:var(--cream);color:var(--ink);
    padding:clamp(90px,12vw,150px) clamp(24px,6vw,80px);
  }
  .system .label{color:rgba(10,10,8,.5);}
  .system-head{max-width:640px;margin-bottom:64px;}
  .system-head h2{
    font-size:clamp(28px,3.6vw,44px);font-weight:500;letter-spacing:-.01em;
    margin-top:16px;line-height:1.15;
  }
  .system-row{
    display:grid;
    grid-template-columns:1fr auto 1fr auto 1fr;
    gap:clamp(14px,2.5vw,28px);
    align-items:center;
    max-width:1060px;
  }
  .sys-card{
    background:var(--black);
    border-radius:18px;
    aspect-ratio:1/1.06;
    position:relative;overflow:hidden;
    display:flex;align-items:center;justify-content:center;
  }
  .sys-caption{
    margin-top:16px;text-align:center;
    font-size:11px;letter-spacing:.32em;text-transform:uppercase;font-weight:500;
  }
  .sys-arrow{
    font-size:22px;color:rgba(10,10,8,.6);
    text-align:center;
  }
  /* card 1: the rush */
  .sys-world{
    position:absolute;inset:0;
    background:
      repeating-linear-gradient(96deg,rgba(255,255,255,.14) 0 2px,transparent 2px 26px),
      repeating-linear-gradient(84deg,rgba(255,255,255,.07) 0 3px,transparent 3px 44px),
      radial-gradient(120% 90% at 50% 110%,rgba(255,255,255,.16),transparent 60%);
    filter:blur(6px);
    animation:rush 7s linear infinite;
  }
  @keyframes rush{from{background-position:0 0,0 0,0 0;}to{background-position:220px 0,-180px 0,0 0;}}
  /* card 2: pause */
  .sys-pause{display:flex;gap:12px;}
  .sys-pause span{
    width:16px;height:104px;background:var(--white);border-radius:3px;
    box-shadow:0 0 14px rgba(255,255,255,.9),0 0 44px rgba(255,255,255,.4);
  }
  /* card 3: new view */
  .sys-view{
    position:absolute;left:-4%;top:50%;transform:translateY(-50%);
    width:104%;height:64%;
    background:var(--rainbow);
    clip-path:polygon(0 47%,100% 0,100% 100%,0 53%);
    filter:blur(9px) saturate(1.2);
  }
  @media(max-width:820px){
    .system-row{grid-template-columns:1fr;max-width:340px;margin:0 auto;}
    .sys-arrow{transform:rotate(90deg);padding:4px 0;}
  }

  /* ---------- RITUAL ---------- */
  .ritual{padding:clamp(90px,12vw,150px) clamp(24px,6vw,80px);}
  .ritual-head{max-width:640px;margin-bottom:72px;}
  .ritual-head h2{
    font-size:clamp(28px,3.6vw,44px);font-weight:500;letter-spacing:-.01em;
    margin-top:16px;line-height:1.15;
  }
  .ritual .label{color:var(--cream-dim);}
  .steps{
    display:grid;
    grid-template-columns:repeat(5,1fr);
    gap:clamp(20px,3vw,40px);
  }
  .step{
    border-top:1px solid var(--cream-faint);
    padding-top:22px;
    transition:border-color .5s;
  }
  .step:hover{border-top-color:var(--cream);}
  .step-num{
    font-size:11px;letter-spacing:.3em;font-weight:500;
    color:var(--cream-dim);
  }
  .step h3{
    font-size:15px;letter-spacing:.22em;text-transform:uppercase;font-weight:500;
    margin:14px 0 12px;
  }
  .step p{
    font-size:14px;line-height:1.7;font-weight:300;color:var(--cream-dim);
  }
  .step:nth-child(4) .step-num{
    background:var(--rainbow-h);
    -webkit-background-clip:text;background-clip:text;color:transparent;
  }
  @media(max-width:980px){.steps{grid-template-columns:repeat(2,1fr);}}
  @media(max-width:540px){.steps{grid-template-columns:1fr;}}

  /* ---------- ICONS ---------- */
  .icons{
    padding:0 clamp(24px,6vw,80px) clamp(90px,12vw,150px);
  }
  .icons-strip{
    border:1px solid var(--cream-faint);
    border-radius:20px;
    padding:clamp(36px,5vw,56px) clamp(20px,4vw,48px);
    display:flex;justify-content:space-between;flex-wrap:wrap;gap:36px;
  }
  .icon-item{
    display:flex;flex-direction:column;align-items:center;gap:18px;
    flex:1;min-width:110px;
  }
  .icon-item svg{
    width:52px;height:52px;overflow:visible;
    filter:drop-shadow(0 0 8px rgba(255,255,255,.5));
  }
  .icon-item .label{color:var(--cream-dim);font-size:10px;}
  .ring-rainbow{filter:drop-shadow(0 0 8px rgba(120,180,255,.5));}

  /* ---------- TAGLINE ---------- */
  .tagline{
    padding:clamp(110px,15vw,190px) clamp(24px,6vw,80px);
    text-align:center;
    position:relative;overflow:hidden;
  }
  .tagline::before{
    content:'';position:absolute;left:50%;top:50%;
    transform:translate(-50%,-50%);
    width:min(900px,120vw);height:60%;
    background:var(--rainbow-h);
    filter:blur(130px);opacity:.14;pointer-events:none;
  }
  .tagline h2{
    position:relative;
    font-size:clamp(32px,6vw,72px);
    font-weight:500;letter-spacing:-.015em;line-height:1.12;
  }
  .tagline h2 span{display:block;color:var(--cream-dim);}

  /* ---------- CTA ---------- */
  .cta{
    background:var(--cream);color:var(--ink);
    padding:clamp(90px,12vw,150px) clamp(24px,6vw,80px);
    text-align:center;
  }
  .cta .label{color:rgba(10,10,8,.5);}
  .cta h2{
    font-size:clamp(30px,4.4vw,54px);font-weight:500;letter-spacing:-.01em;
    margin:16px auto 18px;max-width:700px;line-height:1.12;
  }
  .cta p{
    color:rgba(10,10,8,.62);font-weight:300;max-width:420px;
    margin:0 auto 40px;line-height:1.7;font-size:16px;
  }
  .cta .btn-solid{background:var(--black);border-color:var(--black);color:var(--cream);}
  .cta .btn-solid:hover{box-shadow:0 14px 40px rgba(0,0,0,.28);}
  .cta .btn-ghost{color:var(--ink);border-color:rgba(10,10,8,.25);}
  .cta .btn-ghost:hover{border-color:var(--ink);}
  .cta-btns{display:flex;gap:16px;justify-content:center;flex-wrap:wrap;}

  /* ---------- FOOTER ---------- */
  footer{
    padding:56px clamp(24px,6vw,80px);
    display:flex;justify-content:space-between;align-items:center;
    flex-wrap:wrap;gap:20px;
    border-top:1px solid var(--cream-faint);
  }
  footer .logo{font-size:18px;}
  footer p{font-size:12px;letter-spacing:.06em;color:var(--cream-dim);font-weight:300;}

  @media (prefers-reduced-motion:reduce){
    *,*::before,*::after{animation-duration:.001s!important;animation-delay:0s!important;transition-duration:.001s!important;}
    html{scroll-behavior:auto;}
  }
</style>
</head>
<body>

<nav>
  <a class="logo" href="#">VÜ</a>
  <div class="nav-links">
    <a href="#system">The System</a>
    <a href="#ritual">The Ritual</a>
    <a href="#moments">Moments</a>
    <a class="nav-cta" href="#download">Take a Vü</a>
  </div>
</nav>

<!-- HERO -->
<header class="hero">
  <div class="prism" id="prism" aria-hidden="true">
    <div class="beam"></div>
    <div class="bars">
      <div class="bar"></div>
      <div class="bar"></div>
    </div>
    <div class="spectrum" id="spectrum"></div>
  </div>
  <span class="label">A modern pause ritual</span>
  <h1>one pause changes the view.</h1>
  <p>The world moves fast. You choose to pause. Step outside, take a breath, and come back with a new view.</p>
  <div class="hero-ctas">
    <a class="btn btn-solid" href="#download">Take a Vü</a>
    <a class="btn btn-ghost" href="#system">How it works</a>
  </div>
  <div class="scroll-hint" aria-hidden="true"></div>
</header>

<!-- THE SYSTEM -->
<section class="system" id="system">
  <div class="system-head">
    <span class="label">The system</span>
    <h2>White light goes in.<br>A new view comes out.</h2>
  </div>
  <div class="system-row">
    <div>
      <div class="sys-card"><div class="sys-world"></div></div>
      <div class="sys-caption">World</div>
    </div>
    <div class="sys-arrow" aria-hidden="true">&rarr;</div>
    <div>
      <div class="sys-card"><div class="sys-pause"><span></span><span></span></div></div>
      <div class="sys-caption">Pause</div>
    </div>
    <div class="sys-arrow" aria-hidden="true">&rarr;</div>
    <div>
      <div class="sys-card"><div class="sys-view"></div></div>
      <div class="sys-caption">New view</div>
    </div>
  </div>
</section>

<!-- THE RITUAL -->
<section class="ritual" id="ritual">
  <div class="ritual-head">
    <span class="label">The ritual</span>
    <h2>Five small steps.<br>Ninety seconds outside.</h2>
  </div>
  <div class="steps">
    <div class="step">
      <div class="step-num">01</div>
      <h3>Home</h3>
      <p>The world moves fast. You choose to pause. Open the app when you're ready.</p>
    </div>
    <div class="step">
      <div class="step-num">02</div>
      <h3>Step out</h3>
      <p>Step outside. Leave the rush behind. Tap "I'm outside" to begin.</p>
    </div>
    <div class="step">
      <div class="step-num">03</div>
      <h3>The moment</h3>
      <p>Ninety seconds. Take a breath. Blow. Let go of whatever you were holding.</p>
    </div>
    <div class="step">
      <div class="step-num">04</div>
      <h3>New view</h3>
      <p>How do you feel? Clearer, lighter, focused. Every pause changes your view.</p>
    </div>
    <div class="step">
      <div class="step-num">05</div>
      <h3>Vü moments</h3>
      <p>Real people. Real moments. Outside. Keep a quiet record of every pause.</p>
    </div>
  </div>
</section>

<!-- ICONS -->
<section class="icons" id="moments">
  <div class="icons-strip">
    <div class="icon-item">
      <svg viewBox="0 0 52 52" fill="none" stroke="#fff" stroke-width="2">
        <circle cx="26" cy="26" r="20"/>
      </svg>
      <span class="label">Presence</span>
    </div>
    <div class="icon-item">
      <svg viewBox="0 0 52 52" fill="#fff">
        <rect x="16" y="8" width="7" height="36" rx="2"/>
        <rect x="29" y="8" width="7" height="36" rx="2"/>
      </svg>
      <span class="label">Pause</span>
    </div>
    <div class="icon-item">
      <svg viewBox="0 0 52 52" fill="none" stroke="#fff" stroke-width="2">
        <circle cx="19" cy="26" r="14"/>
        <circle cx="33" cy="26" r="14"/>
      </svg>
      <span class="label">Connection</span>
    </div>
    <div class="icon-item">
      <svg class="ring-rainbow" viewBox="0 0 52 52" fill="none" stroke-width="2.5">
        <defs>
          <linearGradient id="rg" x1="0" y1="0" x2="1" y2="1">
            <stop offset="0" stop-color="#ff3b30"/>
            <stop offset=".25" stop-color="#ff9500"/>
            <stop offset=".5" stop-color="#ffe14d"/>
            <stop offset=".72" stop-color="#4cd964"/>
            <stop offset=".88" stop-color="#38b6ff"/>
            <stop offset="1" stop-color="#af52de"/>
          </linearGradient>
        </defs>
        <circle cx="26" cy="26" r="20" stroke="url(#rg)"/>
      </svg>
      <span class="label">New view</span>
    </div>
    <div class="icon-item">
      <svg viewBox="0 0 52 52" fill="none" stroke="#fff" stroke-width="2">
        <path d="M42 26a16 16 0 1 1-4.7-11.3"/>
        <path d="M38 6v9h-9" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <span class="label">Reset</span>
    </div>
  </div>
</section>

<!-- TAGLINE -->
<section class="tagline">
  <h2>Same world.<span>Different view.</span></h2>
</section>

<!-- CTA -->
<section class="cta" id="download">
  <span class="label">Ready for a pause?</span>
  <h2>A better pause. A better view.</h2>
  <p>VÜ is free on iOS and Android. Take your first pause today — it only takes ninety seconds.</p>
  <div class="cta-btns">
    <a class="btn btn-solid" href="#">Download for iOS</a>
    <a class="btn btn-ghost" href="#">Download for Android</a>
  </div>
</section>

<footer>
  <a class="logo" href="#">VÜ</a>
  <p>a pause can change the view. &nbsp;·&nbsp; © 2026 VÜ</p>
</footer>

<script>
  // Subtle parallax on the hero spectrum — skipped when reduced motion is preferred.
  const spectrum = document.getElementById('spectrum');
  const prism = document.getElementById('prism');
  const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  if (!reduced && spectrum && window.matchMedia('(pointer:fine)').matches) {
    window.addEventListener('mousemove', (e) => {
      const r = prism.getBoundingClientRect();
      const dx = (e.clientX - (r.left + r.width / 2)) / r.width;
      const dy = (e.clientY - (r.top + r.height / 2)) / r.height;
      spectrum.style.transform =
        `translateY(calc(-50% + ${dy * 8}px)) translateX(${dx * 10}px) rotate(${dx * 1.4}deg)`;
    }, { passive: true });
  }
</script>

</body>
</html>
